# Intentionally left empty. Environment variables are managed via the EB Console
# (Configuration → Environment properties). Keeping this file without option_settings
# prevents accidental overrides to blank values during deployments.
